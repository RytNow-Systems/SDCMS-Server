meta {
  name: Get Courier By ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/courier-partners/{{createdCourierId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: isTruthy
  res.body.data.id: eq {{createdCourierId}}
}
