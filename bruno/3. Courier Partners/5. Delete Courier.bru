meta {
  name: Delete Courier
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/courier-partners/{{createdCourierId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: isTruthy
}
