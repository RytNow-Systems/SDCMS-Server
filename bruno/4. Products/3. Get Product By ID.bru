meta {
  name: Get Product By ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/products/{{createdProductId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: isTruthy
  res.body.data.id: eq {{createdProductId}}
}
