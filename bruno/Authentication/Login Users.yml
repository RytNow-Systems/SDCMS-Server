info:
  name: Login Users
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/users/login"
  body:
    type: json
    data: |
      {
        "email": "admin@example.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
