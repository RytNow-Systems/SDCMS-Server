meta {
  name: List Employees
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/employees?page=1&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: isTruthy
  res.body.data: isDefined
  res.body.meta.totalRows: isDefined
}
