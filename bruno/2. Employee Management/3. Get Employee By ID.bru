meta {
  name: Get Employee by ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/employees/{{createdEmployeeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: isTruthy
  res.body.data.id: eq {{createdEmployeeId}}
}
